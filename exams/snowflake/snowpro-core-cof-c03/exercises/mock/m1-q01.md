# M1-Q01 同時workloadの設計

- Layer: mock
- Objective IDs: 1.1
- Question type: single-choice
- Required selections: 1
- Chapter: `textbook/domain-1/01-architecture.md`
- Status: complete

## 問題

ある企業では、朝のETLが実行されると経営dashboardのqueryが遅くなります。両方が同じtableを参照しており、dataの鮮度は共通に保ちたいという要件があります。Snowflakeのアーキテクチャを最も活かす変更はどれですか。

## 選択肢

- A. ETL用Virtual Warehouseだけをsize upし、dashboardも同じwarehouseを使い続ける
- B. 同じmulti-cluster warehouseをETLとdashboardで共有し、最大cluster数を増やす
- C. dashboard用databaseを毎朝cloneし、ETLとは別のdataを参照させる
- D. ETLとdashboardに独立したVirtual Warehouseを割り当て、同じ永続dataを参照させる

## 正解

D. ETLとdashboardに独立したVirtual Warehouseを割り当て、同じ永続dataを参照させる

## 正解理由

Virtual Warehouseはcompute resourceを共有しないためworkloadを分離でき、中央のstorageを共有するためdata copyなしで同じdataを利用できます。

## 各誤答が誤りである理由

- A: ETLの処理時間は短くなる可能性がありますが、両workloadが同じcompute resourceを奪い合う構成は残ります。
- B: Multi-cluster warehouseはconcurrencyを処理できますが、異なるworkloadの性能とcostを独立して管理する要件にはwarehouse分離が適します。
- C: Cloneは作成時点のdataを分岐させるため、ETL後のdata鮮度を自動的に共有する構成ではありません。
- D: Virtual Warehouseごとにcomputeを分離しながら中央storageの同じdataを参照できるため、競合回避と共通のdata鮮度を同時に満たします。

## 周辺知識

compute分離後も、各warehouseのsize、auto-suspend、concurrencyをcostと性能の両面から調整します。

## 解答根拠

- `docs-key-concepts-architecture` — [Snowflake key concepts and architecture](https://docs.snowflake.com/en/user-guide/intro-key-concepts)

## 追加学習

- `docs-compute-cost` — [Understanding compute cost](https://docs.snowflake.com/en/user-guide/cost-understanding-compute)
