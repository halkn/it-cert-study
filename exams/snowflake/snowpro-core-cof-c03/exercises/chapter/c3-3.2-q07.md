# C3-3.2-Q07 Streamのmetadata列
- Layer: chapter
- Objective IDs: 3.2
- Question type: single-choice
- Required selections: 1
- Chapter: `textbook/domain-3/02-automated-ingestion.md`
- Status: complete
## 問題
Standard streamでUPDATE操作はどのように表現されますか。
## 選択肢
- A. `METADATA$ACTION`が`DELETE`の行と`INSERT`の行のペアで表され、両方の`METADATA$ISUPDATE`が`TRUE`になる
- B. `METADATA$ACTION`が`UPDATE`の1行で表される
- C. `METADATA$ISUPDATE`が`TRUE`の1行だけが返り、`METADATA$ACTION`は空になる
- D. 変更前の値だけが`METADATA$ROW_ID`とともに返る

## 正解

A

## 正解理由
`METADATA$ACTION`の値は`INSERT`と`DELETE`だけです。UPDATEは削除行と挿入行のペアとして記録され、`METADATA$ISUPDATE`が`TRUE`になります。
## 各誤答が誤りである理由
- A: UPDATEは`DELETE`と`INSERT`のペアで表され、`METADATA$ISUPDATE = TRUE`で識別します。
- B: `METADATA$ACTION`に`UPDATE`という値はありません。
- C: `METADATA$ACTION`は必ず`INSERT`か`DELETE`の値を持ちます。
- D: 変更前の値だけを返す仕様ではありません。

## 周辺知識
`METADATA$ROW_ID`は行を追跡する一意で不変のIDで、同じ行に対する変更を対応付けるのに使えます。
## 解答根拠
- `docs-streams` — https://docs.snowflake.com/en/user-guide/streams
## 追加学習
- `docs-streams-intro` — https://docs.snowflake.com/en/user-guide/streams-intro
