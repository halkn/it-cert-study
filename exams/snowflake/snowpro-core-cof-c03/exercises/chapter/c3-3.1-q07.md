# C3-3.1-Q07 重複ロードの回避
- Layer: chapter
- Objective IDs: 3.1
- Question type: single-choice
- Required selections: 1
- Chapter: `textbook/domain-3/01-loading-unloading.md`
- Status: complete
## 問題
`COPY INTO <table>`の`FORCE = TRUE`はどのような動作をしますか。
## 選択肢
- A. ロード状態が不明なファイルだけを対象にする
- B. ロード後にstageからファイルを削除する
- C. errorがあってもロードを継続する
- D. load metadataを無視してすべてのファイルをロードする

## 正解

D

## 正解理由
`FORCE = TRUE`はload metadataを参照せず、以前ロード済みかどうかにかかわらずすべてのファイルをロードします。既定は`FALSE`です。
## 各誤答が誤りである理由
- A: それは`LOAD_UNCERTAIN_FILES = TRUE`の動作で、対象は状態が不明なファイルに限られます。
- B: ロード後のファイル削除は`PURGE = TRUE`の動作です。
- C: errorがあっても継続するのは`ON_ERROR = CONTINUE`の動作です。
- D: `FORCE = TRUE`はload metadataを無視するため、同じファイルが再度ロードされ重複データが生じ得ます。

## 周辺知識
Load metadataはtableごとに保持され、ファイルの`LAST_MODIFIED`が64日より古いなどの条件でロード状態が不明になります。重複を避けたい運用で`FORCE`を選ぶのは誤りです。
## 解答根拠
- `docs-copy-into-table` — https://docs.snowflake.com/en/sql-reference/sql/copy-into-table
## 追加学習
- `docs-data-load-overview` — https://docs.snowflake.com/en/user-guide/data-load-overview
